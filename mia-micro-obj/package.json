{
  "name": "mia-micro",
  "version": "1.0.0",
  "dependencies": {
    "isomorphic-unfetch": "^2.0.0"
  },
  "scripts": {
    "start": "node index.js",
    "build": "rm -rf app.zip && yarn install --production && zip -r app.zip node_modules index.js"
  }
}

query PageQuery (
  $pageId: ID!
) {
  page (
    id: $pageId
  ) {
    id
    title
    type
    text
    video
    mainImage {
      id
    }
    comparisonImages {
      id
      index
    }
  }
}

query DetailQuery (
  $detailId: ID!
) {
  detail (
    id: $detailId
  ) {
    id
    title
    image {
      id
      organization {
        id
      }
    }
    clips {
      id
      title
      description
    }
  }
}

query ClipQuery (
  $clipId: ID!
) {
  clip (
    id: $clipId
  ) {
    id
    title
    description
    geometry {
      type
      coordinates
    }
    detail {
      id
      image {
        id
      }
    }
    additionalImages {
      id
    }
  }
}

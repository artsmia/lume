query ClipQuery (
  $clipId: ID!
) {
  clip (
    id: $clipId
  ) {
    id
    title
    description
    detail {
      id
      image {
        id
        organization {
          id
        }
      }
    }
  }
}

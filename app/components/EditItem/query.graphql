query editItemQuery (
  $itemId: ID!
  $userId: ID
  $orgSub: String
) {
  item (id: $itemId) {
    id
    title
    attribution
    culture
    dimensions
    medium
    date
    accessionNumber
    currentLocation
    creditLine
    text
    mainImage {
      id
    }
    details {
      id
      image {
        id
        organization {
          id
        }
      }
    }
  }
  user (id: $userId) {
    id
    email
  }
  organization (
    subdomain: $orgSub
  ) {
    id
    images {
      id
    }
  }
}

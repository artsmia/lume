query DetailQuery (
  $detailId: ID!
) {
  detail (
    id: $detailId
  ) {
    id
    title
    image {
      id
    }
    clips {
      id
      title
      description
      geometry {
        type
        coordinates
      }
    }
  }
}

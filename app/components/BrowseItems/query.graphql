query allItems (
  $orgSub: String
  $search: String
  $filter: Filter
) {
  items (
    search: $search
    orgSub: $orgSub
    filter: $filter
  ) {
    id
    title
    mainImage {
      id
    }
    updatedAt
  }
  organization (
    subdomain: $orgSub
  ) {
    id
  }
}

query itemListData (
  $search: String
  $orgSub: String
) {

  items (
    search: $search
    orgSub: $orgSub
  ) {
    id
    title
    mainImage {
      id
    }
    organizations {
      id
    }
  }
}

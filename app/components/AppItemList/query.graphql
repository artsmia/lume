query itemListData (
  $search: String
  $orgSub: String
  $filter: Filter
) {

  items (
    search: $search
    orgSub: $orgSub
    filter: $filter
  ) {
    id
    title
    mainImage {
      id
    }
    organizations {
      id
    }
  }
}

query ZoomerQuery (
  $imageId: ID!
  $detailId: ID!
) {
  image (
    id: $imageId
  ) {
    id
    organization {
      id
    }
    localId
  }
  detail (
    id: $detailId
  ) {
    id
    image {
      id
      organization {
        id
      }
    }
    geometry {
      type
      coordinates
    }
  }

}

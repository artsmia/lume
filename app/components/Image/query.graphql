query ImageQuery (
  $imageId: ID!
) {
  image (
    id: $imageId
  ) {
    id
    organization {
      id
      customImageApiEnabled
      customImageEndpoint
    }
    host
    gdriveId
    s3Bucket
    title
    alt
    localId
  }
}

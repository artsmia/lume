query booksQuery (
  $search: String
  $filter: Filter
  $orgSub: String
) {
  books (
    search: $search
    filter: $filter
  ) {
    id
    title
    previewImage {
      id
    }
    updatedAt
  }
  organization (
    subdomain: $orgSub
  ) {
    id
  }
}

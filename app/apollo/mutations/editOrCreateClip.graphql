mutation editOrCreateClip (
  $clipId: ID
  $title: String
  $description: String
  $coordinates: String
  $detailId: ID

) {
  editOrCreateClip (
    id: $clipId
    title: $title
    description: $description
    coordinates: $coordinates
    detailId: $detailId
  ) {
    id
    title
    description
    detail {
      id
    }
  }
}

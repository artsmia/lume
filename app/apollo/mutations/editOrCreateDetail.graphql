mutation editOrCreateDetail (
  $detailId: ID
  $itemId: ID
  $title: String
  $imageId: ID
) {
  editOrCreateDetail (
    id: $detailId
    itemId: $itemId
    title: $title
    imageId: $imageId
  ) {
    id
    title
    imageId
  }
}

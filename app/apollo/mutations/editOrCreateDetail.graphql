mutation editOrCreateDetail (
  $detailId: ID
  $itemId: ID
  $title: String
  $imageId: ID
  $newClipDetailId: ID
) {
  editOrCreateDetail (
    id: $detailId
    itemId: $itemId
    title: $title
    imageId: $imageId
    createAndAddClip: {
      detailId: $newClipDetailId
    }
  ) {
    id
    title
    image {
      id
      organization {
        id
      }
    }
    clips {
      id
      title
      description
    }
  }
}
